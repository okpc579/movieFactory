<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">

<select id="findIdByEmailAndName" resultType="String">
	select username from member where email = #{email} and name = #{name}
</select>

<select id="findPasswordByIdAndEmailAndName" resultType="String">
	select password from member where email = #{email} and name = #{name} and username = #{username}
</select>

<update id="updatePassword">
	update member set password = #{newEncodedPassword} where username = #{username}
</update>
</mapper>